import styled from "styled-components";

export const StyledFooter = styled.footer`
  position: relative;
  width: 100%;
  p {
    a {
      font-weight: bold;
      &:hover {
        color: deeppink;
      }
    }
  }
`;
